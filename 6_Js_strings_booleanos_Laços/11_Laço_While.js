//while: Útil quando o número de iterações não é conhecido e depende de uma condição que é verificada antes de cada iteração.
//Sintaxe:
//while (condição) {
// bloco de código a ser executado
//}

let contador = 0

while (contador < 5) {
    console.log(`Resultado da variavel ${contador}.`)
    contador++
}